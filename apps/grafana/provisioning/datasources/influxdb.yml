apiVersion: 1

datasources:
  - name: InfluxDB
    type: influxdb
    access: proxy
    url: http://influxdb:8086
    secureJsonData:
      token: your-super-secret-auth-token
    jsonData:
      version: Flux
      organization: my-org
      defaultBucket: sensor-data
      tlsSkipVerify: true
